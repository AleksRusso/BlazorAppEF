
function displayMessage() {
    // Получаем доступ к элементу <p> по его id
    var outputElement = document.getElementById("output");

    // Изменяем содержимое элемента <p> на "Hello, world!"
    outputElement.innerHTML = "Hello, world!";
}
